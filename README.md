shellconfig
===========

config files


usage:
===========

vim:
`mv ~/.vimrc ~/.vimrc.bak`
`mv ./.vimrc ~/.vimrc`
`mkdir -p ~/.vim/view`

screen:
`mv ~/.screenrc ~/.screenrc.bak`
`mv ./.screenrc ~/.screenrc`
